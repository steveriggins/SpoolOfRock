import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("SpoolOfRock")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
